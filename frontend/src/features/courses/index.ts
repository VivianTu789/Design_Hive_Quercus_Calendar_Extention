/**
 * Course feature entry point.
 *
 * Group members:
 * - The Course data model currently lives in `src/models/course.ts`.
 * - Extend that interface with any additional course fields you need.
 */

export * from '../../models/course';

