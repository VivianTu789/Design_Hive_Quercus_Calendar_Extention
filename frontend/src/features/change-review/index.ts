/**
 * Change Review feature entry point.
 *
 * Group members:
 * - The Change Review Panel popup UI currently lives in `src/components/ChangeReviewPanel.tsx`.
 * - This is the place to:
 *   - Adjust how pending changes are displayed.
 *   - Connect to backend change review endpoints when they are ready.
 */

export { ChangeReviewPanel } from '../../components/ChangeReviewPanel';

