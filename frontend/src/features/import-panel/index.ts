/**
 * Import Panel feature entry point.
 *
 * Group members:
 * - The Import Panel popup UI currently lives in `src/components/ImportPanel.tsx`.
 * - This is where you can:
 *   - Change how courses/assignments are displayed for import.
 *   - Wire the panel up to real backend APIs instead of fake data.
 */

export { ImportPanel } from '../../components/ImportPanel';

